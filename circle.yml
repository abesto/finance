machine:
  node:
    version: 6.1.0

dependencies:
  cache_directories:
    - ~/.meteor
    - ~/.npm
    - .meteor/local
    - node_modules
  pre:
    - "[ -f ~/.meteor/meteor ] || curl https://install.meteor.com | /bin/sh"

test:
  override:
    - ./ci.sh

deployment:
  production:
    branch: master
    heroku:
      appname: wnab
