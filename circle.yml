machine:
  node:
    version: 6.1.0

dependencies:
  cache_directories:
    - ~/.meteor
    - ~/.npm
    - .meteor/local
  pre:
    - "[ -f ~/.meteor/meteor ] || curl https://install.meteor.com | /bin/sh"
    - ~/.meteor/meteor update
    - npm install chimp

test:
  override:
    - ./ci.sh
